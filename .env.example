# === 方式 A：Supabase（家长用短链 ?teacher=xxx 即可看到课表）===
# 1) 打开 https://supabase.com 注册并新建一个 Project
# 2) 左侧 SQL Editor → New query → 粘贴并执行：
#    teacher-booking-app/supabase/migrations/001_teacher_schedules.sql 的全部内容 → Run
# 3) 左侧 Project Settings（齿轮）→ API：
#    复制 Project URL、anon public 密钥（不要泄露 service_role）
# 4) 把本文件复制为 .env.local，填写下面两行，保存后执行 npm run dev（必须重启 dev 才读 env）

VITE_SUPABASE_URL=https://你的项目编号.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9....你的anon公钥

# 5) 部署到 Netlify 等：在控制台 Environment variables 里添加同名两个变量，再重新部署
